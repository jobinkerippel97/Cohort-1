const express = require('express')
const router = express.Router()

router.get('/allMenu', )
router.get('/menu/:Id',)

router.post('/addMenu',)


router.patch('update-Menu',)
router.delete('delete-Menu')

module.exports = { menuRoutes: router }